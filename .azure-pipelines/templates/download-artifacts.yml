parameters:
- name: directoryPath
  type: string

steps:
  - download: none
  - task: DownloadPipelineArtifact@2
    inputs:
      source: current
      artifact: Drop
      path: '${{ parameters.directoryPath }}/'
      patterns: |
        **
        *
